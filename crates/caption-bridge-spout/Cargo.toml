[package]
name = "caption-bridge-spout"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false
description = "Windows Spout2 publisher for Native/GPUI OBS verification. Real send is Windows-only; validation and the Native share name compile everywhere."
license = "MIT"

[dependencies]
thiserror = "2"

[target.'cfg(target_os = "windows")'.dependencies]
spout2-rs = "0.1.1"
