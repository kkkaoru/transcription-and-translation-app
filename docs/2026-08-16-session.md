# 2026-08-16 session

A one-page account of the day’s work. Individual commits already have
reasons. This page is the map: what is still open, what landed after
the first draft, what the contracts guard, and how the gate was
actually run.

HEAD at write time: `d16e428`. Installed desktop bundle replaced at
2026-08-16 08:07 local (`7219c7c`). App was not launched after that
install.
`build_id=b20260815230707709-7219c7ccf06868ce527a7647903f125e5bed4071-ed5c977e`
is in that binary. It is not `unknown`. Later desktop commits exist
on `main` and are **not** in that install. Do not rebuild unless asked;
the 08:07 binary is the live-session target.

## Still open

These are more important than the finished list.

1. **User decision: production Zenz.** Hosted `MODEL_ROUTES` is still
   live `{}`. Local Plan 3 vs Plan 1 is **22/23**. Hosted vs Plan 1 is
   **3/23** because every hosted row is `unconfigured-route`. Turning
   Zenz on needs an owned HTTPS `/completion` host, a live var change,
   and a second-or-later compare utterance with `usedCompletion=true`.
   Hosted ready no longer advertises Zenz ids
   (`models=["azookey-rust-wasm"]` on inference `fb4a685c`, compare
   `c1e711c0`). An unadvertised Zenz convert still returns dictionary +
   `unconfigured-route`. Do not treat 22/23 as deployed quality. See
   `docs/plan3-status.md`.
2. **User decision: whether to bundle GGUF weights.** The installer is
   161 MiB and ships no weights. Bundling small Zenz + Hy-MT2 1.8B Q4
   would add about **+1.12 GiB**. The updater is a full
   `.app.tar.gz`, not a delta. This is **not** the gateway-size
   question.
3. **User decision: whether to keep shipping Bun inside the gateway.**
   `kotoba-inference-gateway` is 60.2 MiB because `bun build --compile`
   embeds JavaScriptCore. The gateway JS is 24.7 KB. Hello-world
   compile is 63,446,114 B; gateway compile is 63,462,626 B. LTO and
   `strip` do not change it. Dropping Bun is ~−60 MiB and breaks the
   single-`.app` contract. A rewrite is months. **60 MiB is Bun;
   +1.12 GiB is GGUF.** See `docs/inference-gateway.md`.
4. **Live speech on the 08:07 build.** That install has
   `app_version` / `build_id`, the startup `runtime config` line,
   caption hold/clear, overflow measurement, and 480 ms turn silence.
   Nobody has spoken into it. Judge still has no post-install session.
   Procedure: `docs/live-session-judge-runbook.md`. Do not launch
   unless asked.
5. `売場` vs `売り場` dictionary cost is closed. Official dict, higher
   value is better: `売り場` -11.359 beats `売場` -12.069. Plan 3's
   greedy completion picked the more expensive surface. `measured-008`
   stays unfixed.
6. Compare idle vs handshake notices were split. An unopened worker path
   now says it is not connected yet; connecting/open without ready still
   waits for advertised models. Unrelated to the live-session judge.
7. Branch coverage is pinned to the 95% floor. Two packages sit at slack
   **0**: worker **853/897 = 95.09%** (this day's fill used the last
   spare) and inference-gateway **221/232 = 95.25%** (already there;
   last `src` change 2026-08-03). Compare and desktop still have slack 2.
   Worker policy is written: cover real Node-reachable branches only, do
   not touch the threshold. Inference-gateway has the same 95 in
   `vitest.config.ts` and **no** such policy. Its 11 misses are defensive
   (`message.text` not a string, log catch, `readyAt == null`,
   `detail || null`, `code ??` / `message ??`, empty trimmed correlation).
   Do not fill those without a policy; that becomes shape-only coverage.
   Options still undecided: keep the floor / raise it / measure new and
   changed files only.

Closed since the first draft of this page, so they must not be
re-opened as work:

- Intern 409-char A/B (`94e887a` vs `1ce052a`): measured
  1582.667 → 1160.822 ms (−26.6%). Kept. Recorded in `a8efb9f`.
- Hy-MT2 7B cost: HEAD (`9200c1e`) shows RSS 4.93 GiB / 424 ms /
  53 tok/s / DL 4.6 GB / 374 s **before** selection. The 08:07 binary
  does **not** contain that warning. Next desktop install will. Not a
  reason to rebuild now.

## Landed after the first draft

| Commit | What |
| --- | --- |
| `77f9b56` / `7219c7c` | Startup line includes `app_version` / `build_id` from the same Vite source as the settings screen. Missing embed is `unknown`, never empty. |
| `9200c1e` | 7B cost visible on the translator `<option>` and the download row before install. |
| `0cb72fe` | Live-session judge runbook. `--after` cuts normalize/turn/translation only. |
| `c1e2217` | Gateway size: 60.2 MiB is Bun, not gateway JS. |
| `d16e428` / `2991e77` | Ready advertises Zenz only when `MODEL_ROUTES` has the id. Compare hides Zenz until ready, then follows advertised ids. Deployed inference `fb4a685c`, compare `c1e711c0` (`page-c1894212ba983beb.js`). |

Deliberately **not** installed after `9200c1e`: putting the 7B warning
on disk would change `build_id` and invalidate the 08:07 session
target. The warning is not a live-session premise.

## How the gate was run

`check:all` was green on this machine nine times on 2026-08-16.
**Every one of those nine executions was started by
`specialist-advisor`.** Timed examples: 107.6 s (`8c81f05`), 121.1 s
(`3f59275`), 110.9 s, 102.7 s (`9200c1e`), 97.6 s (`0cb72fe`),
99.7 s (`c1e2217`), 101.6 s (`d16e428`).

`pi-runner-check-all` announced a start twice and did not run the
gate. `pi-developer-1` produced analysis that entered the main line
(wiring-contract design, intern handoff) and then stopped responding
for long stretches; those tasks were reassigned.

The timing wrapper, wasm digest, CI-superset / drift checks, and
destructive contract probes were written in this checkout by the
developer agents. They are not a substitute for who actually invoked
`bun run check:all`. The next session that uses the same roster should
treat “I started check:all” as unproven until a log file, exit code,
and HEAD stamp exist.

## 1. Five wiring contracts

Each one exists because a type-correct path can still drop the
behavior. Destructive checks were run and then restored.

| # | Guards | Runs in | Failure |
| --- | --- | --- | --- |
| 1 | Desktop always passes `--turn-check-silence-ms 480` to Parapper. Rejects a stale 960 helper default. | `rust:desktop:test` (`gateway.rs` `embedded_parapper_argv_keeps_measured_turn_silence_and_rejects_legacy_override`) | Desktop Rust test fails. Does not inspect a live `ps` argv. |
| 2 | Checked-in Worker wasm exports the lattice ABI (`azookey_lattice_open` / search / close) and `azookey_convert_n_best`. `assets:verify` uses the same `REQUIRED_WASM_EXPORTS` list as parity. | `assets:verify` twice in `check:all` (checkout, then after `worker:typecheck`); `scripts/verify-generated-assets.test.mjs`; `scripts/verify-azookey-wasm-parity.mjs` | `assets:verify must require azookey_lattice_open` or `missing ABI exports: …` |
| 3 | The warmed lazy Worker converter actually has `openLattice`. The wasm export alone is not enough. | `scripts/probe-worker-one-completion.test.mjs` via `worker:test:coverage` | `lazy converter is missing openLattice` |
| 4 | Local `check:all` is a true superset of CI, including the **second** `assets:verify` after `worker:typecheck`. | `scripts/verify-ci-local-gates.test.mjs`, `scripts/run-timed-quality-steps.test.mjs` | Gate-manifest test fails if the post-build verify is dropped |
| 5 | Compare UI JSON keeps a `leftContext` **key** on every Zenz frame, including an empty string. Dictionary frames omit the key. | `apps/azookey-compare/src/lib/worker-client.contract.test.ts` via `azookey-compare:test:coverage` | Frame object has no `leftContext` (the old truthy-drop) |

Related, not one of the five: Zenz and dictionary `leftContext`
contracts fail independently; a missing `azookey_lattice_open` is
named by both assets:verify and parity.

## 2. Observation added today

None of these print speech text, paths, or secrets.

- **Caption residue.** Frontend logs
  `caption display lifecycle=visible|hold|clear age_ms=… generation=…`.
  Judge: stale if hold/visible `age_ms>=8000` (hold design is 5000 ms).
  Hold-then-clear is normal. Hold superseded by a later visible is
  normal. `hold_without_clear` is not. Commit `b5bd7f5`, judge in
  `scripts/judge-live-session.py`.
- **Caption overflow.** Frontend logs
  `caption overflow content_width=… container_width=… overflowed=… line_count=…`.
  Judge splits `line_count=1` (wrap never took effect) from
  `line_count>=2` (wrapped and still did not fit). Those need
  different next actions. Commits `5356819`, `58879b7`, `3a67ca6`.
- **Startup settings and build.** One line at process start, before
  sidecars:
  `runtime config app_version=… build_id=… turn_check_silence_ms=… normalizer=… translator=… hold_clear_ms=… source_max_chars=… translation_max_chars=… streaming_interim_asr=…`.
  Numbers, booleans, and the Vite identity only. Judge prints that
  whole line, or `runtime_config verdict=unknown` if the log predates
  `3091994`. A `3091994` line without identity fields becomes
  `app_version=unknown build_id=unknown`.

Judge criteria were committed **before** any live session
(`ef5bd9c`): SUCCESS_MAX=100, SUCCESS_P95=48, LONG_CHARS=129,
STALE_DISPLAY_MS=8000, SECONDARY_NORMALIZE_P95_MS=3000, n≥20 for
slow normalize.

## 3. Shipped but not actually running (seven)

These are the “we implemented it, then found it was not on the path”
fixes from this day. Each one looked done until a contract, a
measurement, or a binary search proved otherwise.

1. **Compare dropped `leftContext` on empty previous captions**
   (`44dbf20`). A truthy check omitted the key. Worker then skipped
   Zenz as `empty-left-context`. Contract 5 exists because of this.
2. **Max-iter always returned the dictionary baseline** (`541e686`).
   Plan 1 returns the last constrained candidate. Plan 3 now does too.
3. **`usedCompletion` meant “text ≠ baseline”** (`44997cf`). That hid
   “Zenz ran and agreed”. It now means the completion was inspected
   (`iterations > 0`).
4. **1-best compatibility faked `FALLBACK`** (`b91c8e1`).
   `azookey_convert_n_best` is now required ABI. Dead `unpackResult`
   deleted. Status bits: OK=0, FALLBACK=specified dict unused,
   INVALID_UTF8, INVALID_ARGUMENT.
5. **Lazy converter did not forward `openLattice`** (`ea9525e`). Wasm
   had the export; the wrapper dropped it. Contract 3 exists because
   of this. Local `check:all` also dropped the post-build
   `assets:verify` until `bd971fc` (contract 4).
6. **Hy-MT2 2-bit / 1.25-bit were selectable** (`74f4b10`). Files
   download (600 MB / 461 MB) then fail
   `gguf_init_from_reader` tensor offset mismatch. Removed from the
   catalog. 7B stays; it loads. Cost is on HEAD, not on 08:07.
7. **`bun run build:app` pruned the Rust debug cache** (`f05242f`).
   Next `check:all` paid 314 s to rebuild. After later installs,
   `apps/desktop/src-tauri/target` stayed ~6 G (debug present).
   Sharing that same official dictionary via a test-only `OnceLock`
   was also tried and **reverted**: 55.18 s → 55.61 s (`437be4b`).

Nearby display bugs, same class: hold logging restarted the 5 s clear
timer (`f027617`); lifecycle stamps used the wrong clock (`93b4cac`);
desktop used a stale 960 ms turn silence until `be10afb`.

Three catalog shapes, three answers: unloadable 2-bit **removed**;
loadable 7B **warned before select**; advertised-but-unrouted Zenz
**unadvertised** (`d16e428`).

## 4. What we measured

### Plan 3 vs Plan 1

Local, TSV `artificial_left_context`, all 23
`usedCompletion=true`: **22/23** exact. Only `measured-008`
(`青果売り場` vs `青果売場`). Shared misses 012 / 020. Cause of 008:
greedy one completion emitted `売場`; lattice followed that prefix.
Baseline already had `売り場`. Do not fix; it would change the
one-HTTP / 2 s premise.

Hosted WS before the ad change: **23/23** `unconfigured-route`,
`usedCompletion=false`, `conversionStatus=0`. Hosted vs Plan 1
**3/23** (001 / 012 / 014 = dictionary already equals Plan 1).
Hosted wasm sha256 `09483add…` matches the repo (396580 bytes).
After `d16e428` / `2991e77`, hosted ready is wasm-only; an unadvertised
Zenz convert is still dictionary + `unconfigured-route`. Compare page
chunk is `page-c1894212ba983beb.js`.

### Models

| Artifact | Result |
| --- | --- |
| Hy 2-bit / 1.25-bit | Size-correct files. Do not load. Removed. |
| Hy Q4 1.8B | Load 792 ms, 80–110 ms translate, ~166 tok/s, RSS **1.39 GiB** |
| Hy Q4 7B | Loads. 424 ms, 53.2 tok/s, RSS **4.93 GiB**. Download 374 s / 4,624,648,896 B |
| zenz-v2 | Loads on bundled `kotoba-zenz-server` (`/health` 200 in 2 s) |
| Installer | 161 MiB, 0 GGUF. Bundling small+Hy Q4 ≈ +1.12 GiB |
| Gateway sidecar | 60.2 MiB Bun runtime. JS 24.7 KB. See above. |
| Licenses | zenz v3.2 GGUF card `apache-2.0`, no LICENSE file (404). Hy LICENSE.txt Apache-2.0. App redistributes no weights |

### Dictionary intern (`1ce052a`)

`1ce052a` said it was unmeasured and a revert candidate. It stays.
Measured 2026-08-16 from `git archive` copies (shared checkout left
clean), official dictionary, release, same 409-character
`synthetic-live-long-growth` prefix (seed=4), one warmup then n=7,
output 290 characters on both sides:

| Commit | Role | Times (ms) | Median |
| --- | --- | --- | ---: |
| `94e887a` | scratch reuse, before intern | 1557.792, 1558.139, 1576.868, 1582.667, 1591.246, 1607.393, 1623.099 | **1582.667** |
| `1ce052a` | intern readings/surfaces | 1145.377, 1151.484, 1160.591, 1160.822, 1188.185, 1203.277, 1223.116 | **1160.822** |

Delta **-421.845 ms / -26.6%**. Precommitted keep threshold was 3%
(the 4.0% bar that kept `94e887a`). Keep. Do not revert.

### Gate

`rust:azookey:test` is ~54 s and is the official-dictionary quality
gate (119 locked anchors + 303 adversarial), not dictionary-open
cost. Three ignored tests in that crate are intentional manual
benches. Worker branch coverage stayed at the 95% threshold on real
Node-reachable paths. Threshold / excludes / workers-pool were not
changed.

## Where to read next

- What to run after the user speaks:
  `docs/live-session-judge-runbook.md`
- Plan 3 remaining gaps and hosted vs local numbers:
  `docs/plan3-status.md`
- Which GGUFs load, and the 4.93 GiB warning: `docs/llama-runtime.md`
- Gateway 60 MiB vs GGUF +1.12 GiB: `docs/inference-gateway.md`
- Why the 54 s crate test must stay: `docs/development-quality-gates.md`
- Model cards vs what the app ships: `docs/model-licenses.md`
- Live-session judge: `scripts/judge-live-session.py`
